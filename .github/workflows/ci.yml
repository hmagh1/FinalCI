name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    # Services nécessaires pour l'application
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: cruddb
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

      memcached:
        image: memcached:alpine
        ports:
          - 11211:11211

    steps:
      # 1️⃣ Récupérer le code depuis GitHub
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Installer PHP et ses extensions nécessaires
      - name: 🐘 Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo, pdo_mysql, memcached, xdebug

      # 3️⃣ Créer le fichier .env pour configurer la base de données
      - name: 🛠 Set up .env for CI
        run: |
          echo "DB_HOST=127.0.0.1" > .env
          echo "DB_PORT=3306" >> .env
          echo "DB_NAME=cruddb" >> .env
          echo "DB_USER=root" >> .env
          echo "DB_PASS=root" >> .env

      # 4️⃣ Installer les dépendances via Composer
      - name: 📦 Install Composer dependencies
        run: composer install --no-progress --prefer-dist

      # 5️⃣ Attendre que MySQL soit prêt à accepter les connexions
      - name: 🐢 Wait for MySQL to be ready
        run: |
          for i in {1..10}; do
            if mysqladmin ping -h127.0.0.1 -uroot -proot --silent; then
              echo "✅ MySQL is up!"
              break
            fi
            echo "⏳ Waiting for MySQL..."
            sleep 5
          done

      # 6️⃣ Importer le fichier SQL dans la base de données manuellement
      - name: ⚙️ Seed database (init.sql)
        run: |
          echo "⚡ Seeding DB from db/init.sql"
          mysql -h127.0.0.1 -uroot -proot cruddb < db/init.sql

      # 7️⃣ Supprimer le cache de PHPUnit (protection contre tests obsolètes)
      - name: ♻️ Clear PHPUnit cache
        run: rm -f .phpunit.result.cache

      # 8️⃣ Lancer les tests unitaires avec couverture de code
      - name: 🧪 Run PHPUnit tests with coverage
        run: |
          chmod +x vendor/bin/phpunit || true
          XDEBUG_MODE=coverage vendor/bin/phpunit --coverage-clover coverage.xml

      # 9️⃣ Uploader les résultats de couverture vers Codecov
      - name: 📤 Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
          fail_ci_if_error: true
          verbose: true

      # 🔍 Étape de débogage des logs MySQL en cas d’échec
      - name: 🐛 Debug MySQL logs on failure
        if: failure()
        run: docker logs $(docker ps -a -q --filter ancestor=mysql:8.0)
